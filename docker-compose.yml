version: '3.8'

services:
  d2r-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      # Optional: Mount data volume if you want to update data without rebuilding
      - ./scraper/data:/app/scraper/data
